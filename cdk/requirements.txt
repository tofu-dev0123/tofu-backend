# aws-cdk-lib は npm の CDK CLI (package.json: aws-cdk ^2.1122.0) と
# Cloud Assembly スキーマ互換性があるバージョンに固定する。
# 未固定だと CI で新しい版が入り「schema version mismatch」で synth/deploy が失敗する。
aws-cdk-lib==2.254.0
constructs>=10.0.0,<11.0.0
python-dotenv>=1.0.0
